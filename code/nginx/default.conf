server {
    listen 80;
    server_name api.agriconnect.gleeze.com;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl;
    server_name api.agriconnect.gleeze.com;

    ssl_certificate     /etc/letsencrypt/live/api.agriconnect.gleeze.com/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/api.agriconnect.gleeze.com/privkey.pem;

    location / {
        proxy_pass http://app:5000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }
}